server:
  port: 8081
spring:
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
    sentinel:
      transport:
        dashboard: localhost:8090
feign:
  okhttp:
    enabled: true
  sentinel:
    enabled: true
hm:
  db:
    database: hm-item
  swagger:
    package: com.hmall.item.controller
