server:
  port: 8084
spring:
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
    sentinel:
      transport:
        dashboard: localhost:8090
feign:
  okhttp:
    enabled: true
  sentinel:
    enabled: true
hm:
  db:
    database: hm-trade
  swagger:
    package: com.hmall.trade.controller
